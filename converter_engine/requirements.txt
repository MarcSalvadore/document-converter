# --- Document Parsers ---
markitdown[all]
python-docx==1.2.0          # For parsing Microsoft Word (.docx) documents
python-pptx==1.0.2         # For parsing PowerPoint (.pptx) presentations
pypdf==6.15.0                # Core PDF processing library
pdfminer.six==20260107      # Required for PDF layout analysis
pdfplumber==0.11.10          # Advanced PDF parsing (tables and text extraction)
openpyxl==3.1.5             # For parsing Excel (.xlsx) spreadsheets
markdownify==1.2.3         # For converting HTML to Markdown
beautifulsoup4==4.15.0      # For parsing and cleaning HTML
Pillow==12.3.0               # For extracting images from PDF documents

# --- CLI & Terminal UI ---
typer==0.27.1           # For building the command-line interface
rich==15.0.0                # For rich terminal output and progress indicators

# --- REST API & Web Server ---
fastapi==0.141.1            # Core framework for the REST API
uvicorn[standard]==0.52.1   # High-performance ASGI server for FastAPI
python-multipart==0.0.32     # Required by FastAPI for handling file uploads

# --- HTTP Client ---
httpx==0.28.1               # Async HTTP client (used by FastAPI TestClient)

# --- Data Masking (redacted tool) ---
pandas==3.0.5
pymupdf>=1.23.0
pytesseract>=0.3.10
